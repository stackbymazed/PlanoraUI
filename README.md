# Planora Frontend

Frontend web application for Planora - Event Management Platform. Built using Next.js (App Router), Tailwind CSS, and React.

## Getting Started

1. Run `npm install` to download dependencies.
2. Run `npm run dev` to start the development server.
3. Open `http://localhost:3000` to view in your browser.
